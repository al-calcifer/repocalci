C$$$$$
C
C General common blocks 
C
C$$$$

      INTEGER NRA,NLA,NP,NAPL,NMA,NTA,KVC,MAXKB,NATOM,KFLAG,NLR
      INTEGER KEND
      COMMON/KXIS1/NRA,NLA,NP,NAPL,NMA,NTA,KVC,MAXKB,NATOM,KFLAG,NLR,
     & KEND

      INTEGER KB,LCHK,NCHK,KCHK,KTYPE,KT
      INTEGER KT2,ITR,MLIST,NLIST,IPOT,ILJ,KTMAX 
      INTEGER NOA ,LSTEP ,nxmol 
      COMMON/KXIS3/KB,LCHK,NCHK(NSMAX),KCHK,KTYPE(NPMAX),KT(100)
     &,KT2(100),ITR(NPMAX),MLIST(NPMAX),NLIST(NPMAX),IPOT,ILJ,KTMAX 
     &,NOA(NTYPES) ,LSTEP ,nxmol 

      REAL*8 R0,R1,R2,R3
      REAL*8 R4
      COMMON/C0/R0(NPMAX,3),R1(NPMAX,3),R2(NPMAX,3),R3(NPMAX,3)
     &,R4(NPMAX,3)

      REAL*8 HEAD
      COMMON/AXIS1/HEAD(20)

      REAL*8 RNP
      COMMON/CNP/RNP(NPMAX,3)

      REAL*8 R0L
      COMMON/C0L/R0L(NPMAX,3)

      REAL*8 DAX,DAY,DAZ
      COMMON/CDA/DAX(NPMAX),DAY(NPMAX),DAZ(NPMAX)

      REAL*8 RLL,CUBE,CUBE2,TOTE,ECONV,ENPR
      COMMON/XIS3/RLL,CUBE(3),CUBE2(3),TOTE,ECONV,ENPR

      REAL*8 EPSI,SIGMA,XMASS
      COMMON/XIS4/EPSI,SIGMA,XMASS(NTYPES)
cc Acrisio
      REAL*8 TOTEant,EETOT,EETOTant,VOLant,PRESSURE,PRESSURE2
      COMMON/XIS3/TOTEant,EETOT,EETOTant,VOLant,PRESSURE,PRESSURE2
      REAL*8 toteNOAr, pvdw2, toteNOAr2
      COMMON/XIS3/toteNOAr,pvdw2, toteNOAr2
ccc

      REAL*8 DELTA,TSTEP,EPAR1,EPAR2,HDEL,TTIME,TIME 
      COMMON/XIS7/DELTA,TSTEP,EPAR1,EPAR2,HDEL(NSMAX),TTIME,TIME 

      REAL*8 PSEED,GSIG,BET,TR,TEM,WD,PI2,DNLA
      REAL*8 EN,pi,GL
      COMMON/XIS1/PSEED,GSIG,BET,TR,TEM,WD,PI2,DNLA,
     &EN(NSMAX),pi,GL(3*NPMAX)

      REAL*8 F02,F12,F32,F42,F52,DELTSQ
      COMMON/XIS2/F02,F12,F32,F42,F52,DELTSQ

      REAL*8 XKE,XKEA,TTCONV,PCONV
      COMMON/XIS11/XKE(NLMAX),XKEA,TTCONV,PCONV

      REAL*8 ETOT,eatom 
      COMMON/ETT/ETOT(NSMAX),eatom(npmax) 

      REAL*8 QBASE,QA1,QA2,QB1,QB2
      COMMON/CRAN/QBASE,QA1,QA2,QB1,QB2

      INTEGER NR,IREM,KREM,NREM
      COMMON/NNREM/NR,IREM(NPMAX),KREM(NPMAX),NREM

      REAL*8 PENET
      COMMON/PEN/PENET
